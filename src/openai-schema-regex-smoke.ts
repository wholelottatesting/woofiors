export function schemaCompatibilityGreeting(name: string): string {
  return `Hello, ${name}`;
}

export function schemaCompatibilitySum(left: number, right: number): number {
  return left + right;
}
